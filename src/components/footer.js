import Avatar from "../components/avatar"
import { rhythm } from "../utils/typography";
import g from "glamorous";

export default () => (
  <g.Div
    textAlign="center"
    fontSize={rhythm(0.5)}
    paddingTop={rhythm(1)}
  >
    <span>Putu Harum Bawa | Front End Developer</span>
  </g.Div>
);
